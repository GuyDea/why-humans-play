import { describe, expect, it } from 'vitest';
import {
  acceptProposal,
  addAnnotation,
  annotationPlugin,
  corePlugins,
  exportMarkdown,
  getAnnotations,
  getLocks,
  getParkingLot,
  getProposals,
  getRevision,
  insertBlockVariantSet,
  insertInlineVariantSet,
  lockPlugin,
  lockRange,
  lockedText,
  newBeatId,
  newId,
  parseMarkdown,
  pendingProposalIds,
  pickActive,
  proposalPlugin,
  receiveProposal,
  rejectProposal,
  requestProposal,
  revisionPlugin,
  schema,
  setActive,
  unlockRange,
  variantNodeViews,
  variantPlugin,
} from '../src/index.js';

describe('public API', () => {
  it('exports every normative editor-core name', () => {
    expect([
      acceptProposal,
      addAnnotation,
      annotationPlugin,
      corePlugins,
      exportMarkdown,
      getAnnotations,
      getLocks,
      getParkingLot,
      getProposals,
      getRevision,
      insertBlockVariantSet,
      insertInlineVariantSet,
      lockPlugin,
      lockRange,
      lockedText,
      newBeatId,
      newId,
      parseMarkdown,
      pendingProposalIds,
      pickActive,
      proposalPlugin,
      receiveProposal,
      rejectProposal,
      requestProposal,
      revisionPlugin,
      schema,
      setActive,
      unlockRange,
      variantNodeViews,
      variantPlugin,
    ]).not.toContain(undefined);
  });
});
